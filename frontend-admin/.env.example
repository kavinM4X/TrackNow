# Local backend (run: cd backend && npm start)
# VITE_API_URL=http://localhost:5000/api

# Netlify API (after migrating from Render)
# API on Vercel (backend only); frontends stay on Netlify
VITE_API_URL=https://track-now-bay.vercel.app/api

# Client app — public registration / driver links shown in admin
VITE_CLIENT_APP_URL=https://tracknow-client.netlify.app

# Render (legacy)
# VITE_API_URL=https://tracknow-h0h3.onrender.com/api

# Fewer API calls (recommended on MongoDB Atlas + Render free tier)
# VITE_DISABLE_CLICK_LOGS=true
