# Netlify — TrackNow Master Admin
# Connect repo with Base directory: frontend-master-admin

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  # Live API on Render
  VITE_API_URL = "https://tracknow-backend-api.onrender.com/api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
