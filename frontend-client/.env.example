# Local backend (run: cd backend && npm start)
# VITE_API_URL=http://localhost:5000/api

# Netlify API
VITE_API_URL=https://tracknow-api.netlify.app/api

# Fewer API calls (recommended on MongoDB Atlas + Render free tier)
# VITE_DISABLE_CLICK_LOGS=true
